# CVEO Webpage

CVEO 课题组官方网站。本项目基于 [Astro](https://astro.build/) 框架构建，使用 [Tailwind CSS](https://tailwindcss.com/) 进行样式开发。

## 🚀 快速开始

### 1. 环境准备

确保已安装 [Node.js](https://nodejs.org/) (建议版本 v20+)。

### 2. 安装依赖

```bash
npm install
```

### 3. 本地开发

启动开发服务器：

```bash
npm run dev
```

浏览器访问 `http://localhost:4321` 即可预览。

### 4. 构建与预览

构建生产版本：

```bash
npm run build
```

预览构建产物：

```bash
npm run preview
```

## 🛠️ 项目结构

- `src/pages/`: 页面路由
- `src/components/`: 可复用 Astro 组件
- `src/content/`: 内容数据（Markdown 格式）
  - `members/`: 成员信息
  - `papers/`: 论文列表
- `src/data/`: 其他结构化数据（新闻、专利等）
- `public/`: 静态资源（图片、图标等）
- `docs/`: 项目开发与维护文档

## 📝 内容管理

本项目主要通过编辑 Markdown 文件来更新内容：

- **成员更新**: 在 `src/content/members/` 目录下新增或修改 `.md` 文件。
- **论文更新**: 在 `src/content/papers/` 目录下新增或修改 `.md` 文件。
- **新闻更新**: 修改 `src/data/news.md`。
- **专利更新**: 修改 `src/data/patents.md`。

详细内容管理规范请参考 [内容管理规范.md](docs/内容管理规范.md)。

## 🧪 检查工具

运行以下命令进行构建前的质量检查：

```bash
# 执行全部检查 (构建 + HTML 校验 + 链接检查)
npm run check

# 单独检查 HTML 规范
npm run check:html

# 单独检查死链
npm run check:links
```

## 🚢 部署

本项目配置了 GitHub Actions 自动化部署方案。每当代码合并到 `main` 分支时，会自动构建并部署到 GitHub Pages。

详细部署指南请参考 [部署与维护指南.md](docs/部署与维护指南.md)。
