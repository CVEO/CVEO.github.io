---
import BaseLayout from '../layouts/BaseLayout.astro'
import PaperCard from '../components/PaperCard.astro'
import MemberCard from '../components/MemberCard.astro'
import { getCollection } from 'astro:content'
import { loadNews, getLatestNews, formatNewsDate } from '../lib/data/news'
import { formatDate as formatDateUtil } from '../lib/utils/format'
import type { NewsItem, Member } from '../lib/types'

import aboutData from '../data/about.json'
import projectsData from '../data/projects.json'
import awardsData from '../data/awards.json'

const papers = await getCollection('papers') || []
const ourPapers = papers.filter(p => p.id.startsWith('our/'))
const sortedPapers = ourPapers.sort((a,b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime())
const recentPapers = sortedPapers.slice(0, 4)

const members = await getCollection('members') || []

// 加载新闻数据
const allNews = await loadNews()
const newsItems = getLatestNews(allNews, 3)

// 按组筛选成员
function byGroup(g: string): Member[] {
  return members.filter(m => (m.data.group || '').toLowerCase() === g.toLowerCase()).map(m => m.data as Member)
}
---
<BaseLayout title="首页">
  <section class="relative overflow-hidden bg-gradient-to-b from-brand-50 to-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
      <div class="text-center">
        <h1 class="text-3xl sm:text-5xl font-bold text-brand-900 mb-4 py-4">武汉大学 CVEO 课题组</h1>
        <p class="text-xl text-slate-700 mb-6">计算机视觉与地球观测交叉研究团队</p>
        <div class="inline-flex items-center space-x-2 text-slate-600">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
          </svg>
          <span>{aboutData.affiliation}</span>
        </div>
      </div>
    </div>
  </section>

  <section id="about" class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4 scroll-mt-16">
    <!-- <h2 class="text-3xl font-bold text-slate-900 mb-6 flex items-center">
      关于课题组
    </h2> -->
    
    <div class="space-y-6">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
        <div class="p-5 rounded-xl bg-gradient-to-br from-brand-50 to-white border border-brand-100 shadow-sm">
          <h3 class="text-lg font-semibold text-slate-900 mb-3">团队简介</h3>
          <div class="text-slate-700 leading-relaxed text-justify space-y-3">
            {aboutData.introduction.split('\n\n').map(para => (
              <p set:html={para} />
            ))}
          </div>
        </div>
        
        <div class="grid grid-cols-2 gap-3">
          {(aboutData.stats || []).map(stat => (
            <div class="p-4 rounded-lg bg-white border border-slate-200 hover:border-brand-200 hover:shadow-md transition-all duration-300">
              <div class="text-2xl font-bold text-brand-900 mb-1">{stat.value}<span class="text-sm ml-1 font-normal text-slate-500">{stat.unit}</span></div>
              <div class="text-sm text-slate-600 font-medium">{stat.label}</div>
            </div>
          ))}
        </div>
      </div>
      
   

      <section id="team" class="max-w-7xl mx-auto  bg-slate-50 scroll-mt-16">
              <div class="rounded-2xl overflow-hidden shadow-xl border border-slate-200">
        <img 
          src="/assets/avatars/team.png" 
          alt="CVEO课题组团队合影" 
          class="w-full h-auto object-cover"
        />
      </div>   
    <div class="text-center">
      <p class="text-slate-600 mb-2 max-w-2xl mx-auto py-4">
        武汉大学CVEO课题组汇聚了来自计算机视觉与地球观测交叉领域的优秀研究人员，包括教授、研究员、博士生、硕士生以及行业顾问。
      </p>
      <a href="/team" class="inline-flex items-center justify-center px-6 py-3 mb-3 rounded-lg bg-brand-900 text-white hover:bg-brand-800 active:bg-brand-700 active:scale-95 transition-all duration-200 font-medium text-base min-h-[44px] min-w-[120px]">
        查看完整团队
        <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
        </svg>
      </a>
    </div>
  </section>
      
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
        <div class="p-5 rounded-xl bg-gradient-to-br from-brand-green-50 to-white border border-brand-green-100 shadow-sm">
          <h3 class="text-lg font-semibold text-slate-900 mb-3">核心研究领域</h3>
          <div class="space-y-3">
            {(aboutData.researchFields || []).map(field => (
              <div class="flex items-start group">
                <div class="flex-shrink-0 w-9 h-9 rounded-lg bg-brand-green-100 flex items-center justify-center mr-3 group-hover:bg-brand-green-200 transition-colors duration-300">
                  <svg class="w-4 h-4 text-brand-green-900" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                  </svg>
                </div>
                <div>
                  <h4 class="font-semibold text-slate-900 group-hover:text-brand-green-900 transition-colors duration-300">{field.title}</h4>
                  <p class="text-sm text-slate-600 mt-1">{field.description}</p>
                </div>
              </div>
            ))}
          </div>
        </div>
        
        <div class="p-5 rounded-xl bg-gradient-to-br from-slate-50 to-white border border-slate-200">
          <h3 class="text-lg font-semibold text-slate-900 mb-3">团队特色</h3>
          <div class="space-y-3">
            <div class="flex items-start">
              <div class="flex-shrink-0 w-7 h-7 rounded-full bg-brand-100 flex items-center justify-center mr-3">
                <svg class="w-3.5 h-3.5 text-brand-900" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
              </div>
              <div>
                <h4 class="font-medium text-slate-900">产学研结合</h4>
                <p class="text-sm text-slate-600 mt-1">理论研究与产业应用紧密结合，推动科技成果转化</p>
              </div>
            </div>
            <div class="flex items-start">
              <div class="flex-shrink-0 w-7 h-7 rounded-full bg-brand-100 flex items-center justify-center mr-3">
                <svg class="w-3.5 h-3.5 text-brand-900" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197m13.5 0a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
              </div>
              <div>
                <h4 class="font-medium text-slate-900">国际化视野</h4>
                <p class="text-sm text-slate-600 mt-1">与多所国际知名高校保持密切合作交流</p>
              </div>
            </div>
            <div class="flex items-start">
              <div class="flex-shrink-0 w-7 h-7 rounded-full bg-brand-100 flex items-center justify-center mr-3">
                <svg class="w-3.5 h-3.5 text-brand-900" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
                </svg>
              </div>
              <div>
                <h4 class="font-medium text-slate-900">创新驱动</h4>
                <p class="text-sm text-slate-600 mt-1">鼓励学生探索前沿技术，培养创新思维</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section id="news" class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 bg-slate-50 scroll-mt-16">
    <h2 class="text-2xl font-semibold text-slate-900 mb-8">新闻动态</h2>
    <div class="space-y-6">
      {(newsItems || []).map((i: NewsItem) => (
        <article class="rounded-lg border border-slate-200 bg-white p-6 hover:shadow-md transition">
          <div class="flex items-start gap-4">
            <div class="flex-shrink-0">
              <div class="w-16 h-16 rounded-lg bg-brand-50 flex items-center justify-center">
                <span class="text-brand-700 font-semibold">{i.date.split('-')[0]}</span>
              </div>
            </div>
            <div class="flex-grow">
              <h3 class="text-lg font-semibold text-slate-900">{i.text}</h3>
              <p class="mt-2 text-slate-600">{formatNewsDate(i.date)}</p>
            </div>
          </div>
        </article>
      ))}
    </div>
    <div class="mt-8 text-center">
      <a href="/news" class="inline-flex items-center justify-center px-6 py-3 rounded-lg border-2 border-brand-900 text-brand-900 hover:bg-brand-50 hover:border-brand-800 active:bg-brand-100 active:scale-95 transition-all duration-200 font-medium text-base min-h-[44px] min-w-[120px]">
        查看更多新闻
        <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
        </svg>
      </a>
    </div>
  </section>

  <section id="research" class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 scroll-mt-16">
    <h2 class="text-2xl font-semibold text-slate-900 mb-8">科研成果</h2>
    
    <div>
      <h3 class="text-xl font-semibold text-slate-900 mb-6">期刊论文</h3>
    
      <div class="grid grid-cols-1 gap-6">
        {(recentPapers || []).map(p => (
          <PaperCard paper={p} showAbstract={false} showLink={false} />
        ))}
      </div>
      <div class="mt-8 text-center">
        <a href="/research" class="inline-flex items-center justify-center px-6 py-3 rounded-lg border-2 border-brand-900 text-brand-900 hover:bg-brand-50 hover:border-brand-800 active:bg-brand-100 active:scale-95 transition-all duration-200 font-medium text-base min-h-[44px] min-w-[120px]">
          查看更多论文
          <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
          </svg>
        </a>
      </div>
    </div>
  </section>

  

  <section id="projects" class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 scroll-mt-16">
    <h2 class="text-2xl font-semibold text-slate-900 mb-8">在研项目</h2>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      {(projectsData || []).slice(0, 6).map(project => (
        <div class="p-6 rounded-lg border border-slate-200 bg-white hover:shadow-md transition">
          <h3 class="text-lg font-semibold text-slate-900 mb-3">{project.title}</h3>
          <p class="text-slate-700 mb-2">负责人：{project.leader}</p>
          <p class="text-slate-700 mb-4">起止时间：{project.period}</p>
          <span class="inline-block px-3 py-1 rounded-full text-sm bg-blue-50 text-blue-700 border border-blue-200">{project.status}</span>
        </div>
      ))}
    </div>
    <div class="mt-8 text-center">
      <a href="/projects/completed" class="inline-flex items-center justify-center px-6 py-3 rounded-lg border-2 border-brand-900 text-brand-900 hover:bg-brand-50 hover:border-brand-800 active:bg-brand-100 active:scale-95 transition-all duration-200 font-medium text-base min-h-[44px] min-w-[120px]">
        查看已结题项目
        <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
        </svg>
      </a>
    </div>
  </section>

  <section id="awards" class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 scroll-mt-16">
    <h2 class="text-2xl font-bold text-slate-900 mb-8 flex items-center">
      <!-- <span class="w-8 h-1 bg-brand-900 mr-4"></span> -->
      获奖情况
    </h2>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      {awardsData.map(award => (
        <div class="flex items-center p-6 rounded-xl border border-slate-200 bg-white hover:shadow-md transition">
          <div class="w-12 h-12 rounded-full bg-brand-50 flex items-center justify-center mr-6 flex-shrink-0">
            <svg class="w-6 h-6 text-brand-900" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 3v4M3 5h4M6 17v4m-2-2h4m5-16l2.286 6.857L21 12l-5.714 2.143L13 21l-2.286-6.857L5 12l5.714-2.143L13 3z" />
            </svg>
          </div>
          <div>
            <h3 class="font-bold text-slate-900">{award.title}</h3>
            <p class="text-sm text-slate-600 mt-1">{award.level} • {award.year}年</p>
          </div>
        </div>
      ))}
    </div>
  </section>

  <section id="contact" class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 bg-slate-50 scroll-mt-16">
    <h2 class="text-2xl font-semibold text-slate-900 mb-8">联系我们</h2>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
      <div>
        <h3 class="text-lg font-semibold text-slate-900 mb-4">联系方式</h3>
        <div class="space-y-4">
          <div class="flex items-start">
            <svg class="w-5 h-5 text-brand-900 mt-1 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
            </svg>
            <div>
              <p class="font-medium text-slate-900">邮箱</p>
              <p class="text-slate-700">cveo@whu.edu.cn</p>
            </div>
          </div>
          <div class="flex items-start">
            <svg class="w-5 h-5 text-brand-900 mt-1 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
            </svg>
            <div>
              <p class="font-medium text-slate-900">地址</p>
              <p class="text-slate-700">湖北省武汉市武汉大学信息学部</p>
            </div>
          </div>
        </div>
      </div>
      <div>
        <h3 class="text-lg font-semibold text-slate-900 mb-4">研究方向</h3>
        <div class="flex flex-wrap gap-2">
          <span class="px-3 py-1 rounded-full text-sm bg-brand-green-50 text-brand-green-900 border border-brand-green-200">遥感场景理解</span>
          <span class="px-3 py-1 rounded-full text-sm bg-brand-green-50 text-brand-green-900 border border-brand-green-200">弱监督学习</span>
          <span class="px-3 py-1 rounded-full text-sm bg-brand-green-50 text-brand-green-900 border border-brand-green-200">时空大模型</span>
          <span class="px-3 py-1 rounded-full text-sm bg-brand-green-50 text-brand-green-900 border border-brand-green-200">多源数据融合</span>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

